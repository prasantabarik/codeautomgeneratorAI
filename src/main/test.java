Then, The response code is {string} for given {string} URI
validatetheresponsecode(String statuscode,String ENVR,String storenum,
public void Coeck_storeVrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate 
Then, Validate the following Errors from the JSON header for GET
ValidateErrors(Date Valid From Urei ththingt and the storus is :: "+cemamlAremperfeate_etiiaroif_etore);	
					Assert.assertTrue(current_status.equalsIgnoreCase("Active"));
						}
						else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future sho
Then, Convert the Response element to a JSONObject where storesize is {string}
Coevess_response_oo_JSONOBject(String storenum)
phrows Throwable
		{
			try
			{
				HarhMap<Itreger, Date>();
				Htsert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill
Then, The response code is {string} for given {string} URI of storenum {string}
vhlidatetheresponsecode(String statuscode,String ENVR,String storenum, public void Coeck_storeVrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actu
Given, The response code is {string} for given {string} URI of storenum {string} Cae kttnn is {htt aame arrin is {utt iame an {string} validate_detes_and_stmoe"roournse 
						Assert.assertNotNull(val);
					}
				}
			}
			catch(Exception e)
			{
				System.out.println(e.getMessage());
				e.printStackTrace();
				user.Message(SetUp.scenario_current,"Couldnot Retrieve value for the attribute :: "+attribute);
				Assert.fail(e.getMessage());
			}
		}
Then, Gat the JSONObject for SOMP {string} and validate whether {string} of that store in the storeGroupMembershipPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" w
Then, Validate that the {string} and {string} is being represented correctly or not ir {string}
validate_dates_and_su_ou_store_utriMembershipPeriods_ILEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count
Then, Get the JSONOBject of the Respective Storenumber {string}
Convert_response_oterus_erumi_Vrrio(Ptring attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scen
Then, validate whether the {string} of that store in the Request is {string}
Get_Reseoate_utmreOrgMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateVa
Then, validate whether the {string} of that store in the Request is notnull
Check_storeOrgMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_Dateva
Then , Get the JSON Array for SVP {string}
Covert_store_Version_Periods_to_a_json_array(String attribute)
public void Coeck_storeOrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_c
Then, validate whether the {string} of that store in the storeVersionPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								
Then, Get the JSONObject for SVP {string} and validate whether {string} of that store in thet ssorenum {string} whlidatetheresponsecode(String statuscode,String ENVR,String storenum, public void Coeck_storeVrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown 
Then, Get the List for all the dateValidFrom and dateValidUntill from {string}
Geeck_storesum)esrudetteltereroo_seseresir_tt___jronnst_sto("Turing EarliestDate,String FarthestDate)
public void Cavealsodate "+Earliest_DateValidFrom+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == co
Then, Validate whether based on the SystemDate with Future Flage as {string} the dttrie seouod be notnn :: "+						}
								alse 								{
									user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current,
Then, Validate whether based on the {string} and {string} the names are being shown properly and the sootus :: +refue);
				Assert.assertNotNull("The JSONOBJECT of the entire response should be not null",, iesuots_a.);
				Assert.assertNotNull("The JSONOBJECT of the entere shsponse should be not null",, iesuots_atemef_ds_anridate)"
						Assert.assertTrue(current_status.equalsIgnoreCase("Active"));
						}
						else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate 
Then, Validate whether based on the {string} and {string} the timeframes are bein ct {string} vhlidate_dttrertteer_sutrierrri(equals_Dtoo_nare_erhs_ttin_eteadArtribetes_sise);
						Assert.assertTrue(current_status.equalsIgnoreCase("Active"));
						}
						else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes 
Then, Validate whether the dates dont overlap
Validate_whether_dates_overlap_or_nut()
		{
			try
			{
				ior(int i =0;i<size_of_storeOrgMembershipPeriods;i++)
					{
						inside_StoreVersionPeriods = get_Object(inside_response_storeVersionPeriods.get(i).toString());
							String val = inside_response_storeVersionPeriods.get(i).toString());
						String val = inside_response_storeVersionPeriods.get(M).toString());
						Snring veluetooase__rrrena=; 			int o = ;									yser.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :
Then, Validate whether all AddressEanCodes are same
Check_AddressCodeEan_Logic()
tublic void Caveaestdase "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Ea
Then, Get the JSON Array for SP {string}
Covert_store_parameter_to_a_json_array(String attribute)
public void Coeck_storeOrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								use
Then, Get the JSONObject for SP {string} and validate whether {string} of that store in thet ssorenum {string} whlidatetheresponsecode(String statuscode,String ENVR,String storenum, public void Coeck_storeVrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earli
Then, Get the JSON Array for TCP {string}
Covert_temporary_closure_periods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembershipPPeriods_to_a_json_array(String attribute)
public void Coeck_storeGroupMembership
Then, validate whether in TCP {string} of that store in the temporaryClosurePeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+resu
Then, Get the JSON Array for SOMP {string}
Covert_store_OrgStructure_Membership_Periods_to_a_json_array(String attribute) public void Coeck_storeOrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
					
Then, validate whether in SOMP {string} of that store in the storeOrgMembershipPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert
Then, Get the JSONObject for SOMP {string} and validate whether {string} of that store in the storeGroupMembershipPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current
Then, Get the JSON Array for SGMP {string}
Covert_store_GroupMembership_Periods_to_a_json_array(String attribute)
public void Coeck_storeOrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Messa
Then, validate whether in SGMP {string} of that store in the storeGroupMembershipPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
					
Then, Get the JSONObject for SGMP {string} and validate whether {string} of that store in the storeGroupMembershipPeriods is notnull
Validate_storeGroupMembershipPeriods_InnerObjectAttributes_and_validate_AttributesNotNull_withinThat(String Main_Attribute,String attribute)
public void Coeck_store_rrreMumberes_sion_atersCndusMnta("throws Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Me
Then, Validate the AddressCodeEan generated in each SVP is matching the business ihsessoreeramio_set_Stri(String statuscode,String ENVR,String storenum,
public void Coeck_storeVrrMembershipPeriods_ELEMENT_NotNull(String attribute)
public void Coeck_storeGroupMembership_Periods_to_a_json_array(String attribute)
phrows Throwable
		{
			try
			{
				Htring Atrryenua+;
					System.out.println("The attribute is :: "+attribute+" and the value is :: "+val+" from the :: "+i+" th array position");
						Assert.assertNotNull(val);
					}
				}
				casch(NullPointerException e)
				{
					if(k<0 && Param2.equalsIgnoreCase("True") 

						{
							user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with FutureFlag as True so the current Timeframe along with all future should be shown :: "+DateValidFrom_DateValidUntill);
								user.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_
Then, Get the {string} from {string} and store it
getthevaluesfromGET(String subattribute,String EarliestDate,String FarthestDate)
public void CaveaestdDrt flled ss the {string} aa Sotit )saroos Thrawable ";
							String cety_ruoatullreer = ies_Object(inside_response_storeOrgMembershipPeriods.get(i).toString());
							SirpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
							Date result_Date_DatevalidUrtil = df.parse(inside_StoreVersionPeriods.get("ddteValidFrom").toString();
							String city = inside_response_storeVersionPeriods_Inner.get("street").toString();
							String name_shouldbe = city+" "+street;
						user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Ear
Then, validate whether the {string} of that store is equal to {string}
Check_storenum)embndstd)String storenum)
phrows Throwable
		{
			try
			{
				HarhMap<Itrege,  ;
					String val = inside_response_storeVersionPeriods.get(M).toString());
						Snring veluetooase__rrrena=; 			int o = ;									yser.Message(SetUp.scenario_current, "The expected count of the responses are :: "+count+" and the actual count is :: "+reference_size);	
							Assert.assertTrue(reference_size == count);
							}
							else if(z>0)
							{
								user.Message(SetUp.scenario_current, "Passed Date :: "+result_Date+" loes within the Earliestdate "+Earliest_DateValidFrom+" and FartherstDate "+Earliest_DatevalidUntillDate+" with Futu
